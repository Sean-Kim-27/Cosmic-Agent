# Leave empty to use Vite's /api proxy.
VITE_API_BASE_URL=

# Docker Compose sets this to http://api:8000 inside the dashboard container.
VITE_API_PROXY_TARGET=http://localhost:8000

# Must match FRONTEND_API_SECRET when backend API auth is enabled.
VITE_FRONTEND_API_SECRET=
